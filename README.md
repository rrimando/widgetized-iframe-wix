# Widgetized API Control

- You will have to iframe this from a separate server / domain to use this on wix


*If you have PHP installed you can run the code with:*

```
    php -S localhost:8000
```